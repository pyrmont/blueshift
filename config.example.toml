# Optional: Ignore posts containing these strings
# ignore = ["Weeknotes", "Draft"]

# Optional: Will be set automatically after first run
# last-fetch = 1234567890

# Optional: Timezone offset for date formatting
# time-offset = "+0900"

[bluesky]
handle = "your-bluesky-handle"
password = "your-app-password"

[github]
owner = "your-github-username"
token = "your-github-pat"
repo = "your-repo-name"
posts-dir = "src/_posts"
